import 'mocha';
import { expect } from 'chai';


describe('users api tests', () => {
    describe('positive (correct) tests', () => {
        it('creates', async () => {
            await Promise.resolve();
            expect(false).to.equal(true);
        });
    });
});
